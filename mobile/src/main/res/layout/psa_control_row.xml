<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingBottom="@dimen/vertical_padding"
    android:paddingLeft="@dimen/image_view_vertical_padding"
    android:paddingRight="@dimen/image_view_vertical_padding"
    android:paddingTop="@dimen/vertical_padding">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="@dimen/control_row_imageview_size"
        android:layout_height="@dimen/control_row_imageview_size"
        android:contentDescription="@string/icon"
        android:padding="@dimen/control_row_imageview_vertical_padding"
        android:src="@mipmap/open_trunk_button" />

    <TextView
        android:id="@+id/control_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_toEndOf="@id/icon"
        android:layout_toStartOf="@+id/drag_icon"
        android:gravity="center_vertical"
        android:text="@string/control_action"
        android:textColor="@color/black"
        android:textSize="@dimen/psa_56_px" />

    <ImageView
        android:id="@+id/drag_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/control_action"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/control_action"
        android:layout_centerInParent="true"
        android:contentDescription="@string/drag_icon"
        android:src="@mipmap/re_order_list_button" />
</RelativeLayout>